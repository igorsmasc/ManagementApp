export default class User {
    constructor( id, name, email, password, hashUser ) {
        this.id = id;
        this.name = name;
        this.email - email;
        this.password = password;
        this.hashUser = hashUser;
    }
}